<nav class="navbar navbar-default">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <b><%= t('.welcome', user_name: current_user.email) %></b> |

      <% if current_user.admin? %>
        <%= link_to t('.admin_panel'), admin_tests_path, class: 'nav-link' %> |
      <% end %>
      <%= link_to t('.feedback'), new_feedback_path, class: 'nav-link' %> |
      <%= link_to t('.badge_panel'), badges_path, class: 'nav-link' %> |
      <%= link_to t('.log_out'), destroy_user_session_path, data: { method: :delete }, class: 'nav-link' %>
    <% else %>
      <%= link_to t('.sign_up'), new_user_registration_path, class: 'nav-link' %> |
      <%= link_to t('.log_in'), new_user_session_path, class: 'nav-link' %>
    <% end %>
  </span>
</nav>
