<h1><%= t('.header') %></h1>
<p><h3><%= link_to t('.new_badge'), new_admin_badge_path, class: 'btn btn-primary', style: 'color: white' %></h3></p>

<%= render 'shared/errors', resource: @badge %>

<table class="table">
  <thead>
  <tr>
    <th><%= t('.title') %></th>
    <th colspan="2"><%= t('.actions') %></th>
  </tr>
  </thead>

  <%= render @badges %>
</table>