<div class="container">
  <h1><%= t('.header') %></h1>
  <p><h3><%= link_to t('.new_test'), new_admin_test_path, class: 'btn btn-primary', style: 'color: white' %></h3></p>

  <table class="table">
    <thead>
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.level') %></th>
        <th><%= t('.questions') %></th>
        <th colspan="3"><%= t('.actions') %></th>
      </tr>
    </thead>

    <%= render @tests %>
  </table>
</div>